import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.lulapresidente.app',
  appName: 'Lula presidente',
  webDir: 'out',
  bundledWebRuntime: false,
};

export default config;
